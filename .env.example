# Exemple de configuration d'environnement pour C-Chartres Web
# Copier ce fichier en `.env.local` et adapter les valeurs sensibles.

APP_ENV=dev
# Générer une valeur sécurisée: `php -r "echo bin2hex(random_bytes(16));"`
APP_SECRET=CHANGE_ME_SECURE_SECRET
DEFAULT_URI=http://localhost

# Base de données (exemples) - décommentez celui que vous utilisez
#DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_dev.db"
DATABASE_URL="mysql://user:password@127.0.0.1:3306/cchartresweb?serverVersion=10.5.8-MariaDB&charset=utf8mb4"
#DATABASE_URL="postgresql://user:password@127.0.0.1:5432/cchartresweb?serverVersion=16&charset=utf8"

# Transport Messenger (optionnel / doctrine par défaut)
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0

# Mailer (null = pas d'envoi)
MAILER_DSN=null://null

# Pour production, compiler les variables : `composer dump-env prod`
